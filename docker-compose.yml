version: '3.9'
services:
  bot:
    build: .
    container_name: telegram-bot
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
    command: ["node", "main.js"]
